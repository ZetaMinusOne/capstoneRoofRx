version: 1
applications:
# backend:
#   phases:
#     build:
#       commands:
#         - pip3 install --user pipenv
#         - pip3 install virtualenv
#         - amplifyPush --simple
  frontend:
    phases:
      preBuild:
        commands:
          # - cd bit-busters-app
          - npm ci --cache .npm --prefer-offline
      build:
        commands:
          - npm run build
    artifacts:
      baseDirectory: build
      files:
        - '**/*'
    cache:
      paths:
        - .npm/**/*
  appRoot: bit-busters-app